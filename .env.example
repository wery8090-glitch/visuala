# PostgreSQL connection string used by Drizzle and all auth/session routes.
# Set this in the deployment environment; never commit real credentials.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
